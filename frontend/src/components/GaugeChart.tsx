import React from 'react';

const GaugeChart: React.FC<{ value: number; min?: number; max?: number; unit?: string; color?: string }> = () => {
  return <div>GaugeChart (implement me)</div>;
};

export default GaugeChart; 